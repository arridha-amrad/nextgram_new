'use client';

import { Button } from '@nextui-org/react';

const ProfileViewArchiveButton = () => {
  return <Button className="font-semibold">View Archive</Button>;
};

export default ProfileViewArchiveButton;
